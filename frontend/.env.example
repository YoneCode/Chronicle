# Privy application id — create one at https://dashboard.privy.io
VITE_PRIVY_APP_ID=your_privy_app_id

# Deployed ChronicleOmega contract address on Bradbury (chain 4221).
VITE_CONTRACT_ADDRESS=0xyour_contract_address

# GenLayer Bradbury settings (defaults are correct; override only if needed).
VITE_CHAIN_ID=4221
